name: Probe D
on:
  push:
    branches: [main]
concurrency:
  group: probe-d-${{ github.ref_name }}
  cancel-in-progress: true
env:
  REGISTRY: ghcr.io
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - run: echo "probe-d ok ${{ env.REGISTRY }}"
